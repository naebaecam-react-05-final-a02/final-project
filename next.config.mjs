/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: 'abijrjpibayqrzanhqcq.supabase.co',
      },
    ],
  },
};

export default nextConfig;
